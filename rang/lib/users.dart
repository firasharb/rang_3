import 'package:flutter/material.dart';

// Manage authorized client devices to log in this account (Devices: phones, tablets... sharing account)
// Manage authorized client devices to log in this device (Users sharing ring device)

class Users extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Users"),
      ),
    );
  }
}